@model IEnumerable<ProyectoBodegav02.Models.Pedido_Producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pedido.descripcion_Pedido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.nom_Producto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pedido.descripcion_Pedido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.nom_Producto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPedido_Producto }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPedido_Producto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPedido_Producto })
        </td>
    </tr>
}

</table>
